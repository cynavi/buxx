import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: '1f235628',
  appName: 'buxx',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
